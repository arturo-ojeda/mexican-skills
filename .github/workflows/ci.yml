name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  syntax:
    name: Syntax & static checks
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [18, 20, 22]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: node --check (todos los .js)
        run: |
          set -e
          while IFS= read -r f; do
            echo "checking $f"
            node --check "$f"
          done < <(find . -path ./node_modules -prune -o -name '*.js' -print)

      - name: bash -n (todos los .sh)
        run: |
          set -e
          while IFS= read -r f; do
            echo "checking $f"
            bash -n "$f"
          done < <(find . -path ./node_modules -prune -o -name '*.sh' -print)

  preflight:
    name: Preflight & self-test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install buzon-tributario deps
        working-directory: buzon-tributario
        run: npm ci --no-audit --no-fund

      - name: Install constancia-situacion-fiscal deps
        working-directory: constancia-situacion-fiscal
        run: npm ci --no-audit --no-fund

      - name: Install pago-infracciones-jalisco deps
        working-directory: pago-infracciones-jalisco
        run: npm ci --no-audit --no-fund

      - name: buzon-tributario --preflight (espera fallo por env vars faltantes)
        working-directory: buzon-tributario
        run: |
          set +e
          out=$(node ./scripts/check-buzon.js --preflight)
          code=$?
          echo "$out"
          test "$code" -eq 2
          echo "$out" | node -e "let s=''; process.stdin.on('data',c=>s+=c).on('end',()=>{const d=JSON.parse(s); if(d.status!=='preflight_failed'){throw new Error('expected preflight_failed: '+s);}})"

      - name: constancia-situacion-fiscal --preflight (espera fallo por env vars faltantes)
        working-directory: constancia-situacion-fiscal
        run: |
          set +e
          out=$(node ./scripts/sat-flow.js --preflight)
          code=$?
          echo "$out"
          test "$code" -eq 2

      - name: constancia-situacion-fiscal --self-test (artifactsDir cae en CWD)
        run: |
          mkdir -p /tmp/work && cd /tmp/work
          # En macOS /tmp es symlink a /private/tmp, así que process.cwd() lo resuelve.
          # Preguntamos a node cuál es su CWD para comparar manzanas con manzanas.
          expected="$(node -e 'console.log(process.cwd())')"
          out=$(node "$GITHUB_WORKSPACE/constancia-situacion-fiscal/scripts/sat-flow.js" --self-test)
          echo "$out"
          echo "$out" | EXPECTED="$expected" node -e "let s=''; process.stdin.on('data',c=>s+=c).on('end',()=>{const d=JSON.parse(s); if(d.artifactsDir !== process.env.EXPECTED){throw new Error('expected '+process.env.EXPECTED+', got '+d.artifactsDir);}})"

      - name: pago-infracciones-jalisco --preflight (espera fallo por env vars faltantes)
        working-directory: pago-infracciones-jalisco
        run: |
          set +e
          out=$(node ./scripts/jalisco-flow.js --preflight)
          code=$?
          echo "$out"
          test "$code" -eq 2
          echo "$out" | node -e "let s=''; process.stdin.on('data',c=>s+=c).on('end',()=>{const d=JSON.parse(s); if(d.status!=='preflight_failed'){throw new Error('expected preflight_failed: '+s);}})"

      - name: pago-infracciones-jalisco --self-test (artifactsDir cae en CWD)
        run: |
          mkdir -p /tmp/work && cd /tmp/work
          expected="$(node -e 'console.log(process.cwd())')"
          out=$(node "$GITHUB_WORKSPACE/pago-infracciones-jalisco/scripts/jalisco-flow.js" --self-test)
          echo "$out"
          echo "$out" | EXPECTED="$expected" node -e "let s=''; process.stdin.on('data',c=>s+=c).on('end',()=>{const d=JSON.parse(s); if(d.artifactsDir !== process.env.EXPECTED){throw new Error('expected '+process.env.EXPECTED+', got '+d.artifactsDir);}})"

      - name: recibo-cfe --preflight (espera fallo por env vars faltantes)
        working-directory: recibo-cfe
        run: |
          set +e
          out=$(node ./scripts/download-recibo-cfe.js --preflight)
          code=$?
          echo "$out"
          test "$code" -eq 2

      - name: recibo-cfe --self-test
        working-directory: recibo-cfe
        run: node ./scripts/download-recibo-cfe.js --self-test

      - name: constancia-fiscal-extractor preflight (PDF inválido => exit 1)
        run: |
          set +e
          echo "not a pdf" > /tmp/notapdf.txt
          out=$(node ./constancia-fiscal-extractor/scripts/preflight.js /tmp/notapdf.txt)
          code=$?
          echo "$out"
          test "$code" -eq 1

      - name: constancia-fiscal-extractor validate.js (RFC genérico SAT)
        run: |
          out=$(echo '{"rfc":"xaxx010101000","nameOrBusinessName":"PUBLICO EN GENERAL","postalCode":"06100"}' | node ./constancia-fiscal-extractor/scripts/validate.js)
          echo "$out"
          echo "$out" | node -e "let s=''; process.stdin.on('data',c=>s+=c).on('end',()=>{const d=JSON.parse(s); if(d.rfc!=='XAXX010101000' || d.personaType!=='FISICA'){throw new Error(s);}})"

  unit-tests:
    name: Unit tests & SKILL.md lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [18, 20, 22]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Lint estructural de SKILL.md
        run: node ./scripts/lint-skills.js

      - name: Unit tests (constancia-fiscal-extractor)
        working-directory: constancia-fiscal-extractor
        run: node --test test/*.test.js
